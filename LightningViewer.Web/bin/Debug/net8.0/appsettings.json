{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "LightningViewer.Web.Workers": "Information"
    }
  },
  "AllowedHosts": "*",

  "ConnectionStrings": {
    "DefaultConnection": "Host=localhost;Database=lightning_viewer;Username=postgres;Password=99197379"
  },

  "EumetSat": {
    // ⚠️  These are EUMETSAT API credentials — NOT Google OAuth.
    // Register at: https://api.eumetsat.int
    // Then create an API key (Consumer Key / Consumer Secret) in your profile.
    // These are managed via dotnet user-secrets for security.
    "ConsumerKey":    "",
    "ConsumerSecret": "",

    // EUMETSAT OAuth2 token endpoint
    "TokenEndpoint": "https://api.eumetsat.int/token",

    // MTG-LI Level 2 Accumulated Flashes collection
    "CollectionId": "EO:EUM:DAT:0691",

    // Worker polling interval (minutes)
    "PollingIntervalMinutes": 5,

    // Data retention window (hours) — records older than this are deleted
    "RetentionHours": 48
  }
}
